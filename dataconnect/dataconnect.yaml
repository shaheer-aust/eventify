specVersion: "v1"
serviceId: "eventify"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "eventify-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./example"]
